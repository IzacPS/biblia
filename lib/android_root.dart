import 'package:flutter/material.dart';

import 'android_main.dart';

class AndroidRoot extends StatelessWidget {
  const AndroidRoot({super.key});

  @override
  Widget build(BuildContext context) {
    return const AndroidMain();
  }
}
